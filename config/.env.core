# AppGear Core Environment - centralized values shared by all modules
# Replace placeholder values according to the target environment.

# Postgres
POSTGRES_USER=appgear
POSTGRES_PASSWORD=changeme
POSTGRES_DB=appgear

# Platform and routing defaults
APPGEAR_ENV=dev
APPGEAR_BASE_DOMAIN=appgear.local
TIMESTAMP=0000000000

# Identity
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=changeme

# JVM-based services
JAVA_OPTS="-Xms512m -Xmx512m"

# Backstage database
BACKSTAGE_PG_USER=backstage
BACKSTAGE_PG_PASSWORD=changeme

# AI gateway
LITELLM_BASE_URL=http://litellm:4000

# Automation
N8N_PORT=5678
